# Mode Verification Worksheet

Fill in the **Result** and **Status** columns as you complete each mode test in Task 2.

- **Result:** One sentence describing what Copilot returned or did.
- **Status:** ✅ worked as expected / ❌ error (note the error message in the Result column).

---

| Mode | Test prompt used | Result | Status |
|------|-----------------|--------|--------|
| Inline completion | Typed `def calculate_area(` and paused | | |
| Ask | `What does starter/verify.py do?` | | |
| Edit | `Add a docstring to each function.` (discarded after review) | | |
| Plan | `Plan a function that reads a CSV and returns rows where status = active.` | | |
| Agent | `List all Python files in the starter/ folder and describe each one.` | | |
| Inline chat | Selected `add` function → `Ctrl+I` → `Add input validation to reject non-numeric arguments.` (dismissed after review) | | |

---

## Notes

Use this space for anything unexpected — error messages, unexpected behavior, or observations:

```
(your notes here)
```
