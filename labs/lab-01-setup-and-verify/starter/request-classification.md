# Request Classification Worksheet

Classify each task as an **included request** or a **premium request**. Write a one-sentence explanation for each.

Use the cost-aware decision rule: *use the cheapest mode that handles the task; escalate only if the cheaper mode gives an incorrect or incomplete result.*

---

| # | Task description | Classification | Explanation |
|---|-----------------|---------------|-------------|
| 1 | Accept a ghost text completion that suggests a loop body | | |
| 2 | Use Ask mode with the default model to explain a 20-line Python function | | |
| 3 | Run an agent session to scaffold a FastAPI project across 5 new files | | |
| 4 | Use Ask mode with GPT-4o to review the architecture of a microservices design | | |
| 5 | Use Edit mode with the default model to rename a variable in one file | | |

---

## Notes

Any tasks that were hard to classify — explain your uncertainty here:

```
(your notes here)
```
