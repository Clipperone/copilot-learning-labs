# Tests for the users API module
#
# Task: write tests using pytest.
# Cover: valid user retrieval, user not found (404), missing required fields, invalid input types.
# Use bare assert statements — no unittest.TestCase.
# One test function per case. Name each function after the behaviour under test.
